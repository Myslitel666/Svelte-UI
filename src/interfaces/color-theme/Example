export interface IColorThemeStore {
    // Категория для интерактивных элементов управления
    controls: {
        button: {
            background: string;
            textColor: string;
            padding: string;
            borderRadius: string;
        };
        textField: {
            borderColor: string;
            background: string;
            textColor: string;
            padding: string;
        };
    };

    // Категория для обычного контента
    content: {
        block: {
            background: string;
            textColor: string;
            padding: string;
        };
        link: {
            color: string;
            hoverColor: string;
        };
        image: {
            borderColor: string;
            borderRadius: string;
        };
    };

    // Категория для макета
    layout: {
        section: {
            background: string;
            padding: string;
            margin: string;
        };
        header: {
            background: string;
            textColor: string;
            height: string;
        };
        footer: {
            background: string;
            textColor: string;
            height: string;
        };
    };

    // Категория для типографики
    typography: {
        fontFamily: string;
        fontSize: string;
        fontWeight: string;
        lineHeight: string;
    };

    // Категория для медиа
    media: {
        image: {
            borderRadius: string;
            boxShadow: string;
        };
        video: {
            borderColor: string;
            borderRadius: string;
        };
    };

    // Категория для примечаний
    notes: {
        background: string;
        textColor: string;
        borderColor: string;
    };

    // Дополнительные параметры
    themeMode: string;
}
